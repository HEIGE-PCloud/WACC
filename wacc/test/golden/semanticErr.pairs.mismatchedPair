# newpair must match the underlying type of the pair

# Output:
# #semantic_error#

# Exit:
# 200

# Program:

begin
  pair(char, bool) x = newpair(10, 20)
end

In test/wacc_examples/invalid/semanticErr/pairs/mismatchedPair.wacc (line 12, column 20):
type error: expected a pair of a char and a bool (pair(char, bool)) but found a pair of an int and an int (pair(int, int))
> begin
>   pair(char, bool) x = newpair(10, 20)
>                    ^
> end

