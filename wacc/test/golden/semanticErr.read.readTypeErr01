# read into pair type

# Output:
# #semantic_error#

# Exit:
# 200

# Program:

begin
  pair(int, int) p = newpair(1,2);
  read p
end



In test/wacc_examples/invalid/semanticErr/read/readTypeErr01.wacc (line 13, column 8):
type error: only ints and chars may be read, but a pair of an int and an int (pair(int, int)) was found
>   pair(int, int) p = newpair(1,2);
>   read p
>        ^
> end

