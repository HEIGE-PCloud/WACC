# unescaped double quote

# Output:
# #syntax_error#

# Exit:
# 100

# Program:

begin
  char a = '"'
end


(line 12, column 13):
  unexpected """
  expected character or escaped character
  This is a special character, it needs to be escaped by '\'.
  >begin
  >  char a = '"'
               ^
  >end