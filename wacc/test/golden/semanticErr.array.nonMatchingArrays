# Trying to assign the wrong array dimension to a variable, or non-matching pairs.
# Thanks to Ethan Range, Fawwaz Abdullah, Robbie Buxton, and Edward Hartley

# Output:
# #semantic_error#

# Exit:
# 200

# Program:

begin
  int[] a = [1, 2];
  int[][] aa = [a, a];
  int[] b = aa
end


In test/wacc_examples/invalid/semanticErr/array/nonMatchingArrays.wacc (line 15, column 3):
type error: expected an int[] but found an int[][]
>   int[][] aa = [a, a];
>   int[] b = aa
>   ^
> end

