# function call with arguments swapped

# Output:
# #semantic_error#

# Exit:
# 200

# Program:

begin
	int f(int a, bool b) is
		return 0 
	end
	int x = call f(true,1);
	println x
end

	


In test/wacc_examples/invalid/semanticErr/function/functionSwapArgs.wacc (line 15, column 13):
type error: expected an int but found a bool
> 	end
> 	int x = call f(true,1);
>             ^
> 	println x

