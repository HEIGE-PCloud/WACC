# Trying to assign non-matching pairs to eachother.
# Thanks to Ethan Range, Fawwaz Abdullah, Robbie Buxton, and Edward Hartley

# Output:
# #semantic_error#

# Exit:
# 200

# Program:

begin
  pair(int, int) p1 = newpair(0, 0);
  pair(char, char) p2 = newpair('a', 'a');
  p2 = p1
end


Semantic error in test/wacc_examples/invalid/semanticErr/pairs/nonMatchingPairs.wacc (line 15, column 8):
type error: expected a pair of a char and a char (pair(char, char)) but found a pair of an int and an int (pair(int, int))
>   pair(char, char) p2 = newpair('a', 'a');
>   p2 = p1
>        ^
> end

