# type mismatch: bool <- char

# Output:
# #semantic_error#

# Exit:
# 200

# Program:

begin
  bool b = 't'
end


Semantic error in test/wacc_examples/invalid/semanticErr/variables/basicTypeErr05.wacc (line 12, column 12):
type error: expected a bool but found a char
> begin
>   bool b = 't'
>            ^
> end

