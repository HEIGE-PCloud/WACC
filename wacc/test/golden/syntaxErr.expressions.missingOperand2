# operator missing second operand

# Output:
# #syntax_error#

# Exit:
# 100

# Program:

begin
  int b = 2 - 
end


In test/wacc_examples/invalid/syntaxErr/expressions/missingOperand2.wacc (line 13, column 1):
  unexpected keyword end
  expected !, "-", arithmetic operator, boolean, character literal, chr, digit, identifier, integer, len, null, opening parenthesis, ord, or strings
  >  int b = 2 - 
  >end
   ^^^
  >