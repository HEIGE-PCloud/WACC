# operator missing second operand

# Output:
# #syntax_error#

# Exit:
# 100

# Program:

begin
  int b = 2 - 
end


(line 13, column 1):
  unexpected keyword end
  expected !, "(", "-", %, *, /, boolean, character, chr, digit, identifier, integer, len, null, ord, or strings
  >  int b = 2 - 
  >end
   ^^^
  >