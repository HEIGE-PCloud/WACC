# program missing call token at function use

# Output:
# #syntax_error#

# Exit:
# 100

# Program:

begin
  int f() is
    return 0 
  end
  int x = f()
end



In test/wacc_examples/invalid/syntaxErr/function/functionMissingCall.wacc (line 15, column 11):
  unexpected "f()"
  function calls may not appear in expressions and must use `call`
  >  end
  >  int x = f()
             ^^^
  >end