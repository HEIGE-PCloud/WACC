# program missing call token at function use

# Output:
# #syntax_error#

# Exit:
# 100

# Program:

begin
  int f() is
    return 0 
  end
  int x = f()
end



(line 15, column 12):
  unexpected "()"
  expected !=, ";", "[", %, &&, *, +, -, /, <, <=, ==, >, >=, end, or ||
  >  end
  >  int x = f()
              ^^
  >end