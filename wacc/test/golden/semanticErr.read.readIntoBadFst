# If we are reading into a pair projection, the element must be readable

# Output:
# #semantic_error#

# Exit:
# 200

# Program:

begin
  pair(bool, bool) p = null;
  read fst p
end

Semantic error in test/wacc_examples/invalid/semanticErr/read/readIntoBadFst.wacc (line 13, column 8):
type error: only ints and chars may be read, but a bool was found
>   pair(bool, bool) p = null;
>   read fst p
>        ^
> end

